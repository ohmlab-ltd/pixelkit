# PixelKit portable UI — dev configuration.
# Copy to .env.local to override. In the packaged app the UI is served by the
# engine itself and no env is needed.

# Origin of the local engine (FastAPI).
NEXT_PUBLIC_API_URL="http://localhost:8001"
