# PixelKit engine — portable build. torch/torchvision first (macOS: default
# wheels include MPS; CUDA boxes: pick the matching cu12x index or let the
# installer script handle it).
torch>=2.4
torchvision>=0.19
transformers>=5.14
accelerate>=1.0
huggingface_hub>=0.26
fastapi>=0.115
uvicorn>=0.30
sse-starlette>=2.1
python-multipart>=0.0.9
python-dotenv>=1.0
numpy>=1.26
Pillow>=10.4
pillow-heif>=0.18
opencv-python-headless>=4.10
blurhash>=1.1
orjson>=3.10
msgpack>=1.0
psutil>=5.9
requests>=2.31
